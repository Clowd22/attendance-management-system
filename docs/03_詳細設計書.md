# 勤怠管理システム 詳細設計書（Phase 0：基盤構築・認証）

| 項目 | 内容 |
|------|------|
| 文書番号 | DD-001 |
| バージョン | 0.1 |
| 作成日 | 2026-09-01 |
| ステータス | レビュー中（未承認） |
| 適用範囲 | Phase 0（基盤構築・認証） |
| 作成者 | Claude Code（開発メンバー） |

---

## 変更履歴

| 版 | 日付 | 変更内容 | 承認 |
|----|------|----------|------|
| 0.1 | 2026-09-01 | 初版作成（Phase 0：基盤構築・認証） | - |

---

## 目次

1. [この文書の使い方](#1-この文書の使い方)
2. [実装全体像](#2-実装全体像)
3. [ステップ0：事前準備](#3-ステップ0事前準備)
4. [ステップ1：設定モジュール（app/core/config.py）](#4-ステップ1設定モジュールappcoreconfigpy)
5. [ステップ2：DB基盤（app/db/base.py・app/db/session.py）](#5-ステップ2db基盤appdbbasepyappdbsessionpy)
6. [ステップ3：ORMモデル（app/models/）](#6-ステップ3ormモデルappmodels)
7. [ステップ4：Alembic マイグレーション](#7-ステップ4alembic-マイグレーション)
8. [ステップ5：パスワード管理（app/core/security.py）](#8-ステップ5パスワード管理appcoresecuritypy)
9. [ステップ6：ログインフォーム定義（app/schemas/auth.py）](#9-ステップ6ログインフォーム定義appschemasauthpy)
10. [ステップ7：リポジトリ層（app/repositories/）](#10-ステップ7リポジトリ層apprepositories)
11. [ステップ8：サービス層（app/services/auth_service.py）](#11-ステップ8サービス層appservicesauth_servicepy)
12. [ステップ9：認証の依存性注入（app/api/routes/deps.py）](#12-ステップ9認証の依存性注入appapiroutesdepspy)
13. [ステップ10：テンプレート（app/templates/）](#13-ステップ10テンプレートapptemplates)
14. [ステップ11：ルーター層（app/api/routes/）](#14-ステップ11ルーター層appapiroutes)
15. [ステップ12：アプリのエントリポイント（app/main.py）](#15-ステップ12アプリのエントリポイントappmainpy)
16. [ステップ13：シードデータ（app/seed.py）](#16-ステップ13シードデータappseedpy)
17. [ステップ14：動作確認](#17-ステップ14動作確認)
18. [受け入れ条件チェックリスト](#18-受け入れ条件チェックリスト)
19. [よくあるつまずきと対処](#19-よくあるつまずきと対処)
20. [次のステップ](#20-次のステップ)

---

## 1. この文書の使い方

### 1.1 対象読者

- Web アプリケーション開発・Python・FastAPI の**初学者**を想定している
- 基本的な Python 構文（変数・関数・クラス・import）は分かる前提とする

### 1.2 この文書で作るもの

Phase 0 のゴールは **「ログインができる」**。具体的には以下を実装する。

- FastAPI アプリの土台（`app/` パッケージ）
- SQLite データベース + ORM モデル（部署・勤務体系・休暇種別・社員）
- 社員ID + パスワードによるログイン / ログアウト / アクセス制御
- ログイン画面・ホーム画面・エラーページ

### 1.3 進め方の約束

1. **ステップ0 から 14 まで順番に**進める。各ステップの末尾に「確認」があり、そこで動作確認する
2. コードは**ファイル全文**を示している。ファイルの置き場所（パス）を確認してから丸ごとコピーする
3. コピーだけでなく、**コメントと解説を読んで理解**する。分からない単語が出たら 1.4 を見る
4. エラーが出たら「19. よくあるつまずきと対処」を確認し、それでも解決しない場合は Claude Code に質問する

### 1.4 用語の簡単な説明

| 用語 | 意味 |
|------|------|
| ルーター | URL（例 `/login`）と処理を対応付ける部品。リクエストの「受け口」 |
| サービス | ビジネスルール（ログイン判定など）を書く部品 |
| リポジトリ | データベースへの読み書きだけを担当する部品 |
| モデル | データベースのテーブルを表す Python クラス |
| 依存性注入（DI） | 関数の引数で「必要な部品を渡してもらう」仕組み。FastAPI が `Depends(...)` で自動的に渡す |
| セッション | ログイン状態をブラウザの Cookie に記憶する仕組み |
| ORM | SQL を直接書かず、Python オブジェクトで DB を操作する仕組み（ここでは SQLAlchemy） |
| マイグレーション | テーブルの作成・変更を履歴として管理する仕組み（Alembic） |

---

## 2. 実装全体像

### 2.1 完成時のファイル構成

```
TimPoPro-USMR/
├── app/
│   ├── __init__.py                    # 空ファイル（app をパッケージにする）
│   ├── main.py                        # ★ステップ12  アプリの入口
│   ├── seed.py                        # ★ステップ13  ダミーデータ投入
│   ├── core/
│   │   ├── __init__.py                # 空ファイル
│   │   ├── config.py                  # ★ステップ1   環境変数の読み込み
│   │   ├── security.py                # ★ステップ5   パスワードのハッシュ化
│   │   └── templates.py               # ★ステップ11  Jinja2 テンプレート設定
│   ├── db/
│   │   ├── __init__.py                # 空ファイル
│   │   ├── base.py                    # ★ステップ2   モデルの親クラス
│   │   └── session.py                 # ★ステップ2   DB エンジン・セッション
│   ├── models/
│   │   ├── __init__.py                # ★ステップ3   モデル一括読み込み
│   │   ├── department.py              # ★ステップ3   部署
│   │   ├── work_schedule.py           # ★ステップ3   勤務体系
│   │   ├── leave_type.py              # ★ステップ3   休暇種別
│   │   └── employee.py                # ★ステップ3   社員（ユーザー）
│   ├── schemas/
│   │   ├── __init__.py                # 空ファイル
│   │   └── auth.py                    # ★ステップ6   ログインフォーム検証
│   ├── repositories/
│   │   ├── __init__.py                # 空ファイル
│   │   └── employee_repository.py     # ★ステップ7   社員の DB アクセス
│   ├── services/
│   │   ├── __init__.py                # 空ファイル
│   │   └── auth_service.py            # ★ステップ8   ログイン判定
│   ├── api/
│   │   ├── __init__.py                # 空ファイル
│   │   └── routes/
│   │       ├── __init__.py            # 空ファイル
│   │       ├── deps.py                # ★ステップ9   認証ガード（DI）
│   │       ├── auth.py                # ★ステップ11  ログイン・ログアウト
│   │       └── home.py                # ★ステップ11  ホーム画面
│   └── templates/
│       ├── base.html                  # ★ステップ10  共通レイアウト
│       ├── login.html                 # ★ステップ10  ログイン画面
│       ├── home.html                  # ★ステップ10  ホーム画面
│       └── errors/
│           ├── 403.html               # ★ステップ10  権限エラー
│           └── 404.html               # ★ステップ10  ページ不存在
├── alembic/                           # ★ステップ4  マイグレーション（自動生成）
├── data/                              # ★ステップ0  SQLite DB 保存先（git管理外）
├── .env                               # ★ステップ0  環境変数
├── .env.example
├── .gitignore
├── README.md
└── requirements.txt
```

### 2.2 実装順序の一覧

| ステップ | 内容 | 作るファイル |
|:---:|------|------|
| 0 | 事前準備 | requirements.txt 追記・.env・data/ |
| 1 | 設定読み込み | core/config.py |
| 2 | DB 基盤 | db/base.py, db/session.py |
| 3 | ORM モデル | models/ 一式 |
| 4 | マイグレーション | alembic/ |
| 5 | パスワード管理 | core/security.py |
| 6 | フォーム検証 | schemas/auth.py |
| 7 | DB アクセス | repositories/employee_repository.py |
| 8 | ログイン判定 | services/auth_service.py |
| 9 | 認証ガード | api/routes/deps.py |
| 10 | 画面テンプレート | templates/ 一式 |
| 11 | ルーター | api/routes/auth.py, home.py, core/templates.py |
| 12 | アプリ入口 | main.py |
| 13 | ダミーデータ | seed.py |
| 14 | 動作確認 | - |

> **なぜこの順序か**: 依存関係のないもの（設定・DB基盤）→ モデル → 認証ロジック → 画面 → 組み立て → データ、という順で積み上げると、動かしながら進められる。

---

## 3. ステップ0：事前準備

### 3.1 仮想環境と依存パッケージ

すでに `.venv` がある場合はここを飛ばしてよい。無ければ以下を実行する。

```bash
# 仮想環境を作成
python3 -m venv .venv
```

### 3.2 requirements.txt に追記（重要）

**セッション管理（SessionMiddleware）は `itsdangerous` ライブラリが必要**だが、現状の requirements.txt には無い。
`requirements.txt` の末尾に以下を追記する。

```text
# --- セッション署名 ----------------------------------------------------------
# Starlette の SessionMiddleware（署名付きクッキー）が依存するライブラリ。
# これが無いと import 時に ModuleNotFoundError になる。
itsdangerous>=2.1
```

追記したらインストールする。

```bash
.venv/bin/pip install -r requirements.txt
```

### 3.3 環境変数ファイル（.env）

`.env` が無ければコピーする。

```bash
cp .env.example .env
```

`.env` の中身（デフォルトのままでも動作する）。

```text
APP_ENV=dev
DEBUG=True
SECRET_KEY=change-me-please
DATABASE_URL=sqlite:///./data/attendance.db
```

> ⚠️ `SECRET_KEY` は開発中はこの値で動くが、**実際に公開する運用では必ずランダムな文字列に変更**すること。

### 3.4 データベース保存ディレクトリの作成

SQLite は `data/` フォルダを自動で作らない。先に作っておく。

```bash
mkdir -p data
```

### 3.5 確認

```bash
ls -d .venv data
# 両方のフォルダが表示されれば OK
```

---

## 4. ステップ1：設定モジュール（app/core/config.py）

### 4.1 役割

`.env` に書いた環境変数をアプリから使えるようにする。pydantic-settings というライブラリを使う。

### 4.2 作成するファイル

`app/core/config.py`

### 4.3 コード

```python
# =============================================================================
# アプリケーション設定
# -----------------------------------------------------------------------------
# .env ファイルに書いた環境変数を Python の設定オブジェクトとして読み込む。
# pydantic-settings の BaseSettings を使うと、環境変数の型変換や必須チェックを
# 自動でやってくれる。
# =============================================================================
from functools import lru_cache

from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    """アプリ全体の設定値。

    .env の「大文字の変数名」が「小文字のプロパティ名」に対応する。
    例: APP_ENV  -> app_env
         DEBUG    -> debug
    """

    #: 実行環境（dev / test / prod）
    app_env: str = "dev"

    #: デバッグモード。True のとき詳細なエラーログを出す
    debug: bool = True

    #: セッションの署名に使う秘密鍵（本番では必ず変更する）
    secret_key: str = "change-me-please"

    #: DB 接続文字列
    database_url: str = "sqlite:///./data/attendance.db"

    # SettingsConfigDict で .env ファイルの読み込み方を指定する
    model_config = SettingsConfigDict(
        env_file=".env",          # プロジェクトルートの .env を読む
        env_file_encoding="utf-8",
        extra="ignore",           # 未知の環境変数は無視する
    )


@lru_cache
def get_settings() -> Settings:
    """設定オブジェクトをキャッシュして返す。

    lru_cache を付けることで、何度呼んでも 1 回だけ Settings を作成する。
    （毎回 .env を読み直す無駄を省く）
    """
    return Settings()
```

### 4.4 解説

- **`BaseSettings`**: pydantic-settings の基底クラス。これを継承すると環境変数を自動で読む
- **`model_config`**: 読み込み方の設定。`env_file=".env"` でカレントディレクトリの `.env` を参照
- **`get_settings()`**: アプリのどこからでも `from app.core.config import get_settings` して使う

### 4.5 確認

```bash
.venv/bin/python -c "from app.core.config import get_settings; print(get_settings().database_url)"
# 出力: sqlite:///./data/attendance.db  が表示されれば OK
```

> 上のコマンドはプロジェクトルートで実行すること。

---

## 5. ステップ2：DB基盤（app/db/base.py・app/db/session.py）

### 5.1 役割

- `base.py`: 全 ORM モデルの親クラス `Base` を定義する
- `session.py`: データベースへの接続（エンジン）と、セッションを作る工場（SessionLocal）を定義する

### 5.2 作成するファイル

`app/db/base.py` / `app/db/session.py`

### 5.3 コード：`app/db/base.py`

```python
# =============================================================================
# SQLAlchemy の宣言的ベース
# -----------------------------------------------------------------------------
# すべての ORM モデルはこの Base を継承して作る。
# Base.metadata に登録されたテーブル情報が、Alembic のマイグレーションに使われる。
# =============================================================================
from sqlalchemy.orm import DeclarativeBase


class Base(DeclarativeBase):
    """全モデルの親クラス。テーブル定義の共通基盤としてだけ使う。"""

    pass
```

### 5.4 コード：`app/db/session.py`

```python
# =============================================================================
# データベース接続（エンジン）とセッション
# -----------------------------------------------------------------------------
# SQLAlchemy の「エンジン」は DB への接続を管理する。
# 「セッション」は 1 つの処理単位（トランザクション）を扱う窓口。
# =============================================================================
from sqlalchemy import create_engine, event
from sqlalchemy.orm import sessionmaker

from app.core.config import get_settings

# .env から設定を取得
settings = get_settings()

# SQLite は別スレッドからの接続を拒否するため、check_same_thread=False を指定する
# （FastAPI は複数スレッドで処理するため、これが無いとエラーになる）
connect_args = {}
if settings.database_url.startswith("sqlite"):
    connect_args["check_same_thread"] = False

# エンジンを作成（接続の窓口となる親玉）
engine = create_engine(settings.database_url, connect_args=connect_args)

# SQLite はデフォルトで外部キー制約が無効になっているため、
# 接続のたびに PRAGMA で有効化する
if settings.database_url.startswith("sqlite"):
    @event.listens_for(engine, "connect")
    def _enable_sqlite_foreign_keys(dbapi_connection, _connection_record):
        """SQLite の外部キー制約を有効化するイベントハンドラ。"""
        cursor = dbapi_connection.cursor()
        cursor.execute("PRAGMA foreign_keys=ON")
        cursor.close()


# セッションを生成する工場。リクエストごとに SessionLocal() を呼ぶ
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)


def get_db():
    """FastAPI の依存性注入で使う、DB セッションの取得関数。

    リクエストごとに 1 つのセッションを作り、処理が終わったら必ず閉じる。
    yield の前後で「開く / 閉じる」を確実に実行するのがポイント。
    """
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

### 5.5 解説

- **`create_engine`**: 接続文字列（URL）からエンジンを作る。エンジンは接続プールを管理する
- **`sessionmaker`**: セッションを作る「工場」。`SessionLocal()` と書くと新しいセッションが返る
- **`get_db`**: FastAPI の `Depends(get_db)` で使う。ルーターの関数が `db: Session = Depends(get_db)` と宣言すると、処理中だけ有効なセッションが渡され、終了時に自動で閉じられる

### 5.6 確認

```bash
.venv/bin/python -c "from app.db.session import engine, SessionLocal; print('DB OK')"
# DB OK と出れば OK
```

> この時点ではテーブルはまだ無い。データベースの実体は次ステップで作る。

---

## 6. ステップ3：ORMモデル（app/models/）

### 6.1 役割

データベースのテーブルを Python クラスとして定義する。1 テーブル = 1 クラス。

### 6.2 作成するファイル

`app/models/__init__.py` / `app/models/department.py` / `app/models/work_schedule.py` / `app/models/leave_type.py` / `app/models/employee.py`

### 6.3 コード：`app/models/department.py`

```python
# =============================================================================
# 部署モデル
# =============================================================================
from sqlalchemy import String
from sqlalchemy.orm import Mapped, mapped_column, relationship

from app.db.base import Base


class Department(Base):
    """部署マスタ。社員が所属する部署を表す。"""

    __tablename__ = "departments"

    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    code: Mapped[str] = mapped_column(String(20), unique=True, nullable=False)  # 部署コード（重複不可）
    name: Mapped[str] = mapped_column(String(100), nullable=False)              # 部署名
    note: Mapped[str | None] = mapped_column(String(255), nullable=True)        # 備考

    # この部署に所属する社員一覧（Employee 側の department と相互に参照）
    employees: Mapped[list["Employee"]] = relationship(back_populates="department")
```

### 6.4 コード：`app/models/work_schedule.py`

```python
# =============================================================================
# 勤務体系モデル
# =============================================================================
from datetime import time

from sqlalchemy import Integer, String, Time
from sqlalchemy.orm import Mapped, mapped_column, relationship

from app.db.base import Base


class WorkSchedule(Base):
    """勤務体系マスタ。所定労働時間などの勤務基準を表す。"""

    __tablename__ = "work_schedules"

    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    code: Mapped[str] = mapped_column(String(20), unique=True, nullable=False)  # 体系コード（重複不可）
    name: Mapped[str] = mapped_column(String(100), nullable=False)              # 体系名
    start_time: Mapped[time] = mapped_column(Time, nullable=False)              # 所定始業時刻（例 09:00）
    end_time: Mapped[time] = mapped_column(Time, nullable=False)                # 所定終業時刻（例 18:00）
    required_minutes: Mapped[int] = mapped_column(Integer, nullable=False)      # 所定労働時間（分）
    break_minutes: Mapped[int] = mapped_column(Integer, nullable=False, default=0)  # 休憩時間（分）
    note: Mapped[str | None] = mapped_column(String(255), nullable=True)        # 備考

    # この勤務体系を適用される社員一覧
    employees: Mapped[list["Employee"]] = relationship(back_populates="work_schedule")
```

> **設計メモ**: 時間は「分」の整数で管理する（480 = 8時間）。float で持つと丸め誤差が出るため、今後の勤怠計算もこの方針で統一する。

### 6.5 コード：`app/models/leave_type.py`

```python
# =============================================================================
# 休暇種別モデル
# =============================================================================
from sqlalchemy import Boolean, Integer, String
from sqlalchemy.orm import Mapped, mapped_column

from app.db.base import Base


class LeaveType(Base):
    """休暇種別マスタ。年次有給休暇・慶弔休暇などの区分を表す。"""

    __tablename__ = "leave_types"

    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    code: Mapped[str] = mapped_column(String(20), unique=True, nullable=False)  # 種別コード（重複不可）
    name: Mapped[str] = mapped_column(String(100), nullable=False)              # 種別名
    grant_days: Mapped[int | None] = mapped_column(Integer, nullable=True)      # 年間付与日数（任意）
    carryover: Mapped[bool] = mapped_column(Boolean, nullable=False, default=False)  # 繰越可否
    half_day: Mapped[bool] = mapped_column(Boolean, nullable=False, default=False)   # 半日休暇可否
    note: Mapped[str | None] = mapped_column(String(255), nullable=True)        # 備考
```

### 6.6 コード：`app/models/employee.py`

```python
# =============================================================================
# 社員モデル（= ログインユーザー）
# =============================================================================
from datetime import date, datetime

from sqlalchemy import Boolean, Date, DateTime, ForeignKey, String
from sqlalchemy.orm import Mapped, mapped_column, relationship

from app.db.base import Base


class Employee(Base):
    """社員マスタ。ログインID（社員番号）とパスワードもこのテーブルで持つ。"""

    __tablename__ = "employees"

    # --- ロールの定数 ---
    ROLE_ADMIN = "admin"        # 管理者（人事・総務）
    ROLE_EMPLOYEE = "employee"  # 一般社員

    # --- 雇用形態の定数（DBにはコードで保存し、画面表示時に日本語へ変換する） ---
    EMPLOYMENT_TYPE_LABELS = {
        "seishain": "正社員",
        "keiyaku": "契約社員",
        "arbeit": "アルバイト",
    }

    id: Mapped[int] = mapped_column(primary_key=True, autoincrement=True)
    employee_number: Mapped[str] = mapped_column(String(20), unique=True, nullable=False)  # 社員番号（=ログインID）
    last_name: Mapped[str] = mapped_column(String(50), nullable=False)     # 姓
    first_name: Mapped[str] = mapped_column(String(50), nullable=False)    # 名
    last_name_kana: Mapped[str] = mapped_column(String(100), nullable=False)  # 姓フリガナ
    first_name_kana: Mapped[str] = mapped_column(String(100), nullable=False) # 名フリガナ

    department_id: Mapped[int] = mapped_column(ForeignKey("departments.id"), nullable=False)  # 所属部署
    position: Mapped[str | None] = mapped_column(String(100), nullable=True)  # 役職（任意）
    employment_type: Mapped[str] = mapped_column(String(20), nullable=False)  # 雇用形態
    work_schedule_id: Mapped[int] = mapped_column(ForeignKey("work_schedules.id"), nullable=False)  # 勤務体系
    hire_date: Mapped[date] = mapped_column(Date, nullable=False)          # 入社日
    retire_date: Mapped[date | None] = mapped_column(Date, nullable=True)  # 退職日（入るとログイン不可）
    email: Mapped[str | None] = mapped_column(String(255), nullable=True)  # メールアドレス（任意）

    role: Mapped[str] = mapped_column(String(20), nullable=False, default=ROLE_EMPLOYEE)  # ロール
    password_hash: Mapped[str] = mapped_column(String(255), nullable=False)  # パスワードハッシュ
    is_active: Mapped[bool] = mapped_column(Boolean, nullable=False, default=True)  # ログイン可フラグ

    created_at: Mapped[datetime] = mapped_column(DateTime, nullable=False, default=datetime.now)  # 作成日時
    updated_at: Mapped[datetime] = mapped_column(
        DateTime, nullable=False, default=datetime.now, onupdate=datetime.now
    )  # 更新日時

    # 関連オブジェクト（外部キー先のテーブルをオブジェクトとして参照できる）
    department: Mapped["Department"] = relationship(back_populates="employees")
    work_schedule: Mapped["WorkSchedule"] = relationship(back_populates="employees")

    @property
    def full_name(self) -> str:
        """姓 + 名 を連結して返す（例: テスト 太郎）。"""
        return f"{self.last_name} {self.first_name}"

    def is_admin(self) -> bool:
        """管理者かどうかを判定する。"""
        return self.role == self.ROLE_ADMIN

    def can_login(self) -> bool:
        """ログイン可能か判定する（有効かつ退職していない）。"""
        if not self.is_active:
            return False
        if self.retire_date is not None and self.retire_date <= date.today():
            return False
        return True
```

### 6.7 コード：`app/models/__init__.py`

```python
# =============================================================================
# モデル一括読み込み
# -----------------------------------------------------------------------------
# このファイルを import するだけで、すべてのモデルが Base.metadata に登録される。
# Alembic のマイグレーションや seed はここを経由してモデルを認識する。
# =============================================================================
from app.db.base import Base
from app.models.department import Department
from app.models.employee import Employee
from app.models.leave_type import LeaveType
from app.models.work_schedule import WorkSchedule

__all__ = ["Base", "Department", "Employee", "LeaveType", "WorkSchedule"]
```

### 6.8 解説

- **`Mapped[int]` / `mapped_column()`**: SQLAlchemy 2.x の記法。カラムの型を Python の型ヒントで表す
- **`ForeignKey("departments.id")`**: 他テーブルへの参照。これで部署IDが整合性チェックされる
- **`relationship()`**: テーブル間の関連を Python オブジェクトでたどれるようにする。`employee.department.name` のように書ける
- **`Mapped[str | None]`**: 空欄（NULL）を許可するカラム。`str | None` と書く
- **`can_login()`**: ログイン可否の判定ロジック。サービス層から呼ぶ

### 6.9 確認

```bash
.venv/bin/python -c "from app.models import Employee, Department, WorkSchedule, LeaveType; print('モデル OK:', len(Employee.__table__.columns), 'カラム')"
# モデル OK: 19 カラム  と出れば OK
```

---

## 7. ステップ4：Alembic マイグレーション

### 7.1 役割

モデル定義（Python コード）から、実際の SQLite テーブルを作る。スキーマ変更を履歴管理する。

### 7.2 手順

#### (1) Alembic の初期化

```bash
.venv/bin/alembic init alembic
```

`alembic/` フォルダと `alembic.ini` が作られる。

#### (2) 接続先の設定（alembic.ini）

`alembic.ini` を開き、`sqlalchemy.url` の行を編集する（既存のコメント行を置き換える）。

```ini
sqlalchemy.url = sqlite:///./data/attendance.db
```

> **メモ**: これは `.env` の `DATABASE_URL` と同じ値にする。二重管理になるが、Phase 0 ではシンプルさを優先する。

#### (3) モデルの認識（alembic/env.py）

`alembic/env.py` を開き、次の 2 箇所を編集する。

**① ファイル先頭の import 部分**（`import sys` などがある行の直後）に追加:

```python
import os
import sys

# プロジェクトルートを import パスに追加する（このファイルは alembic/ の中にあるため）
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

# モデルを読み込んで metadata に登録する（全モデルの import が走る）
from app.models import Base  # noqa: E402
```

**② `target_metadata` の行**（`target_metadata = None` と書いてある部分）を置き換え:

```python
target_metadata = Base.metadata
```

#### (4) マイグレーションスクリプトの生成

モデル定義から変更を自動検出して、スクリプトを作る。

```bash
.venv/bin/alembic revision --autogenerate -m "Phase0: 人事マスタと社員テーブル"
```

`alembic/versions/xxxx_Phase0....py` が生成される。

#### (5) 適用（テーブル作成）

```bash
.venv/bin/alembic upgrade head
```

### 7.3 確認

```bash
.venv/bin/alembic current
# 生成したリビジョンが表示されれば OK
ls data/
# attendance.db が作成されていれば OK
```

SQLite にテーブルができているか直接確認したい場合:

```bash
.venv/bin/python -c "from sqlalchemy import inspect; from app.db.session import engine; print(inspect(engine).get_table_names())"
# ['departments', 'employees', 'leave_types', 'work_schedules'] が出れば OK
```

---

## 8. ステップ5：パスワード管理（app/core/security.py）

### 8.1 役割

パスワードを平文で保存せず、**bcrypt でハッシュ化**して保存・照合する。

### 8.2 作成するファイル

`app/core/security.py`

### 8.3 コード

```python
# =============================================================================
# パスワードのハッシュ化・照合
# -----------------------------------------------------------------------------
# pwdlib というライブラリを使う。
# 注意: PasswordHash.recommended() は Argon2 を返すため、要件どおり bcrypt を
#       明示的に指定する（rounds=12 は pwdlib のデフォルトと同じ）。
# =============================================================================
from pwdlib import PasswordHash
from pwdlib.hashers.bcrypt import BcryptHasher

# bcrypt 方式でハッシュ化するオブジェクト（rounds=12 = 計算コスト）
_password_hash = PasswordHash([BcryptHasher(rounds=12)])


def hash_password(password: str) -> str:
    """パスワードをハッシュ化して文字列で返す。保存時・シード時に使う。"""
    return _password_hash.hash(password)


def verify_password(plain_password: str, hashed_password: str) -> bool:
    """平文パスワードが、保存済みハッシュと一致するか検証する。

    ハッシュ化してから比較するのではなく、bcrypt の内部で検証する。
    （ハッシュにはランダムな salt が含まれるため、単純な文字列比較はできない）
    """
    return _password_hash.verify(plain_password, hashed_password)
```

### 8.4 解説

- **`PasswordHash([BcryptHasher(rounds=12)])`**: ハッシュ方式を bcrypt に固定する。`recommended()` は Argon2 なので使わない（要件定義は bcrypt 指定）
- **ハッシュは salt 付き**: 同じパスワードでも毎回違うハッシュ文字列になる。だから照合は `verify_password()` を使う

### 8.5 確認

```bash
.venv/bin/python -c "from app.core.security import hash_password, verify_password; h=hash_password('password123'); print('ハッシュ:', h); print('一致:', verify_password('password123', h)); print('不一致:', verify_password('wrong', h))"
# 一致: True / 不一致: False  と出れば OK
```

---

## 9. ステップ6：ログインフォーム定義（app/schemas/auth.py）

### 9.1 役割

ログイン画面から送られてくるフォーム入力を検証する。

### 9.2 作成するファイル

`app/schemas/auth.py`

### 9.3 コード

```python
# =============================================================================
# ログインフォームのスキーマ
# -----------------------------------------------------------------------------
# Pydantic の BaseModel で入力の検証ルールを定義する。
# ルーターで request.form() から受け取った値と突き合わせる。
# =============================================================================
from pydantic import BaseModel, Field


class LoginForm(BaseModel):
    """ログイン画面の入力項目。"""

    #: 社員番号（ログインID）。空文字は許可しない
    employee_number: str = Field(min_length=1, max_length=20)

    #: パスワード。空文字は許可しない
    password: str = Field(min_length=1, max_length=128)
```

### 9.4 解説

- **`Field(min_length=1, ...)`**: 空のまま送信されたら `ValidationError` になる
- この段階では「形式の検証」だけを行う。ID・パスワードが**正しいか**はサービス層（ステップ8）で判定する

---

## 10. ステップ7：リポジトリ層（app/repositories/）

### 10.1 役割

データベースへの**読み書きだけ**を担当する。ここにはビジネスルールを書かない。

### 10.2 作成するファイル

`app/repositories/employee_repository.py`

### 10.3 コード

```python
# =============================================================================
# 社員のデータアクセス（リポジトリ）
# -----------------------------------------------------------------------------
# 「DB からどう取り出すか」を担当する。ビジネスルール（ログイン判定など）は
# ここには書かず、サービス層に置く。
# =============================================================================
from sqlalchemy import select
from sqlalchemy.orm import Session

from app.models.employee import Employee


class EmployeeRepository:
    """社員テーブルに対する読み書きをまとめたクラス。"""

    def __init__(self, db: Session):
        self.db = db

    def get_by_employee_number(self, employee_number: str) -> Employee | None:
        """社員番号で社員を 1 件検索する。見つからなければ None。"""
        stmt = select(Employee).where(Employee.employee_number == employee_number)
        return self.db.execute(stmt).scalar_one_or_none()

    def get_by_id(self, employee_id: int) -> Employee | None:
        """主キー（id）で社員を 1 件取得する。見つからなければ None。"""
        return self.db.get(Employee, employee_id)
```

### 10.4 解説

- **`select(Employee).where(...)`**: SQLAlchemy 2.x の SELECT 文。`db.execute()` で実行する
- **`scalar_one_or_none()`**: 「0 件または 1 件」を想定して結果を取り出す。2 件以上あると例外になる（社員番号は unique なので安全）
- **`db.get(Employee, id)`**: 主キーでの単純取得のショートカット

---

## 11. ステップ8：サービス層（app/services/auth_service.py）

### 11.1 役割

ビジネスルール（ログインできるかどうか）を判定する。

### 11.2 作成するファイル

`app/services/auth_service.py`

### 11.3 コード

```python
# =============================================================================
# 認証サービス
# -----------------------------------------------------------------------------
# 「ログインできるか」という業務ルールを担当する。
# DB へのアクセスはリポジトリに任せ、ここでは判定ロジックだけを持つ。
# =============================================================================
from sqlalchemy.orm import Session

from app.core.security import verify_password
from app.models.employee import Employee
from app.repositories.employee_repository import EmployeeRepository


class AuthService:
    """認証（ログイン判定）のサービス。"""

    def __init__(self, db: Session):
        self.db = db
        self.employee_repo = EmployeeRepository(db)

    def authenticate(self, employee_number: str, password: str) -> Employee | None:
        """社員番号とパスワードを検証し、成功したら Employee を返す。

        失敗理由は呼び出し側に教えず、常に None を返す（情報漏えい防止）。
        """
        # 1. 社員番号で社員を探す
        employee = self.employee_repo.get_by_employee_number(employee_number)
        if employee is None:
            return None

        # 2. ログイン可能か（有効・退職していないか）を確認
        if not employee.can_login():
            return None

        # 3. パスワードの照合
        if not verify_password(password, employee.password_hash):
            return None

        # すべてのチェックを通過したら社員を返す
        return employee
```

### 11.4 解説

- 失敗理由を区別しないで `None` を返すのがセキュリティ上のポイント。「IDが違う」「パスワードが違う」を教えない
- 判定ロジックはすべてここに集約される。テストしやすい構造

---

## 12. ステップ9：認証の依存性注入（app/api/routes/deps.py）

### 12.1 役割

「この画面はログインが必須」「この画面は管理者のみ」という**ガード**を、画面ルーターに簡単に適用するための仕組み。

### 12.2 作成するファイル

`app/api/routes/deps.py`

### 12.3 コード

```python
# =============================================================================
# 認証ガード（依存性注入）
# -----------------------------------------------------------------------------
# ルーターの関数の引数に Depends(get_current_user) と書くだけで、
# 「未ログインならログイン画面へ」という制御ができる。
# =============================================================================
from fastapi import Depends, HTTPException, Request
from sqlalchemy.orm import Session
from starlette.responses import RedirectResponse

from app.db.session import get_db
from app.models.employee import Employee


def get_current_user(
    request: Request,
    db: Session = Depends(get_db),
) -> Employee:
    """ログイン中の社員を返す。未ログインならログイン画面へ飛ばす。

    ルーターで `current_user: Employee = Depends(get_current_user)` と
    宣言すると、この関数が自動的に実行される。
    """
    # 1. セッションから user_id を取り出す（ログイン時に保存した値）
    user_id = request.session.get("user_id")
    if user_id is None:
        # 未ログイン → ログイン画面へリダイレクト
        raise RedirectResponse(url="/login", status_code=303)

    # 2. user_id で社員を取得
    employee = db.get(Employee, user_id)
    if employee is None or not employee.can_login():
        # 社員が存在しない・無効 → セッションをクリアしてログイン画面へ
        request.session.clear()
        raise RedirectResponse(url="/login", status_code=303)

    # 3. ログイン中の社員を返す（画面側で current_user として使う）
    return employee


def require_admin(
    current_user: Employee = Depends(get_current_user),
) -> Employee:
    """管理者専用画面のガード。管理者以外は 403 を返す。

    使い方: `admin: Employee = Depends(require_admin)`
    """
    if not current_user.is_admin():
        raise HTTPException(status_code=403, detail="権限がありません")
    return current_user
```

### 12.4 解説

- **`raise RedirectResponse(...)`**: FastAPI では Response を raise できる。ここでログイン画面へ転送する
- **`Depends(get_current_user)`**: チェーンが効く。`require_admin` は内部で `get_current_user` を先に実行する
- 一般社員用の画面には `Depends(get_current_user)`、管理者用の画面には `Depends(require_admin)` を付ける

---

## 13. ステップ10：テンプレート（app/templates/）

### 13.1 役割

画面（HTML）をサーバー側で生成する。Jinja2 の「テンプレート継承」で共通部分をまとめる。

### 13.2 作成するファイル

`app/templates/base.html` / `login.html` / `home.html` / `errors/403.html` / `errors/404.html`

### 13.3 コード：`app/templates/base.html`

```html
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}勤怠管理システム{% endblock %}</title>
    <style>
        /* 最小限のスタイル（本格的な見た目は Phase 1 で整備する） */
        body { font-family: sans-serif; margin: 0; padding: 0; color: #333; }
        header { background: #2c3e50; color: #fff; padding: 12px 24px;
                 display: flex; justify-content: space-between; align-items: center; }
        header h1 { font-size: 18px; margin: 0; }
        header a { color: #fff; text-decoration: none; }
        main { max-width: 960px; margin: 0 auto; padding: 24px; }
        footer { text-align: center; color: #999; padding: 20px; font-size: 12px; }
        .error { color: #c0392b; background: #fdecea; border-radius: 4px; padding: 10px; }
        .card { border: 1px solid #ddd; border-radius: 8px; padding: 24px; max-width: 420px; }
        label { display: block; margin-bottom: 14px; }
        input { padding: 8px; width: 100%; box-sizing: border-box; max-width: 280px; }
        button { padding: 8px 28px; cursor: pointer; }
    </style>
</head>
<body>
    <header>
        <h1>勤怠管理システム</h1>
        {# ログイン中（current_user が渡されている）ならログアウトリンクを表示 #}
        {% if current_user %}
            <a href="/logout">ログアウト</a>
        {% endif %}
    </header>

    <main>
        {# 各画面の中身はここに差し込まれる #}
        {% block content %}{% endblock %}
    </main>

    <footer>v0.1</footer>
</body>
</html>
```

> Jinja2 では `{% ... %}` が制御文、`{{ ... }}` が値の表示、`{# ... #}` がコメント。値は自動で HTML エスケープされる（XSS 対策）。

### 13.4 コード：`app/templates/login.html`

```html
{# base.html を継承して、content の中身だけを定義する #}
{% extends "base.html" %}

{% block content %}
<div class="card">
    <h2>ログイン</h2>

    {# 認証エラーがあれば赤いメッセージを表示 #}
    {% if error %}
        <p class="error">{{ error }}</p>
    {% endif %}

    {# action を明示してログイン処理（POST）へ送る #}
    <form method="post" action="/login">
        <label>社員番号
            <input type="text" name="employee_number" maxlength="20" autofocus>
        </label>
        <label>パスワード
            <input type="password" name="password" autocomplete="current-password">
        </label>
        <button type="submit">ログイン</button>
    </form>
</div>
{% endblock %}
```

### 13.5 コード：`app/templates/home.html`

```html
{% extends "base.html" %}

{% block content %}
<h2>ホーム</h2>
<p>こんにちは、<strong>{{ current_user.full_name }}</strong> さん</p>
<p>所属: {{ current_user.department.name }}</p>
<p>ロール:
    {% if current_user.is_admin() %}管理者{% else %}一般社員{% endif %}
</p>
<p class="muted">（打刻ボタン・月次サマリは Phase 2 以降で追加します）</p>
{% endblock %}
```

### 13.6 コード：`app/templates/errors/403.html`

```html
{% extends "base.html" %}

{% block content %}
<h2>403 - アクセス権限がありません</h2>
<p>このページへのアクセスは許可されていません。</p>
<p><a href="/">ホームへ戻る</a></p>
{% endblock %}
```

### 13.7 コード：`app/templates/errors/404.html`

```html
{% extends "base.html" %}

{% block content %}
<h2>404 - ページが見つかりません</h2>
<p>お探しのページは存在しないか、移動しました。</p>
<p><a href="/">ホームへ戻る</a></p>
{% endblock %}
```

### 13.8 解説

- **テンプレート継承**: `base.html` の `{% block content %}` の部分に、各画面の中身が埋まる
- **`{{ current_user.full_name }}`**: ルーターから渡した `current_user` オブジェクトのプロパティを表示
- **`{{ current_user.department.name }}`**: relationship で部署オブジェクトまでたどって表示できる

---

## 14. ステップ11：ルーター層（app/api/routes/）

### 14.1 役割

URL と処理を対応付ける。リクエストの受け付け、サービスの呼び出し、画面の返却までを行う。

### 14.2 作成するファイル

`app/api/routes/auth.py` / `app/api/routes/home.py`（`deps.py` はステップ9で作成済み）

### 14.3 まずテンプレートの共通設定（`app/core/templates.py`）

`auth.py` と `home.py` の両方で使うテンプレートオブジェクトを共通化するため、先に作成する。

```python
# =============================================================================
# Jinja2 テンプレートの設定
# -----------------------------------------------------------------------------
# templates オブジェクトはルーター（auth.py / home.py）で共有する。
# テンプレートのフォルダは app/templates を絶対パスで指定する。
# =============================================================================
import os

from fastapi.templating import Jinja2Templates

# このファイルは app/core/templates.py なので、2つ上の app/ がルートになる
_APP_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

# app/templates をテンプレートフォルダに指定する
templates = Jinja2Templates(directory=os.path.join(_APP_DIR, "templates"))
```

### 14.4 コード：`app/api/routes/auth.py`

```python
# =============================================================================
# 認証ルーター（ログイン・ログアウト）
# =============================================================================
from fastapi import APIRouter, Depends, Request
from pydantic import ValidationError
from sqlalchemy.orm import Session
from starlette.responses import RedirectResponse

from app.core.templates import templates
from app.db.session import get_db
from app.models.employee import Employee
from app.schemas.auth import LoginForm
from app.services.auth_service import AuthService

# /login などの URL をこのルーターが担当する
router = APIRouter()


@router.get("/login")
async def login_form(request: Request):
    """ログイン画面を表示する。"""
    # すでにログイン済みならホームへ飛ばす（二重ログイン防止）
    if request.session.get("user_id"):
        return RedirectResponse(url="/", status_code=303)

    return templates.TemplateResponse(request, "login.html", {"error": None})


@router.post("/login")
async def login(request: Request, db: Session = Depends(get_db)):
    """ログイン処理。フォームの内容を検証して認証する。"""
    # 1. フォーム（HTML の入力値）を受け取る
    form = await request.form()

    # 2. Pydantic スキーマで入力の形式を検証（空欄など）
    try:
        login_data = LoginForm(
            employee_number=form.get("employee_number", ""),
            password=form.get("password", ""),
        )
    except ValidationError:
        return templates.TemplateResponse(
            request,
            "login.html",
            {"error": "社員IDとパスワードを入力してください"},
            status_code=400,
        )

    # 3. サービス層で認証（存在・有効・パスワード照合）
    auth_service = AuthService(db)
    employee = auth_service.authenticate(
        login_data.employee_number, login_data.password
    )

    # 4. 認証失敗 → 同じ画面にエラーメッセージを表示
    if employee is None:
        return templates.TemplateResponse(
            request,
            "login.html",
            {"error": "社員IDまたはパスワードが正しくありません"},
            status_code=401,
        )

    # 5. 認証成功 → セッションに社員IDを保存してホームへ
    request.session["user_id"] = employee.id
    return RedirectResponse(url="/", status_code=303)


@router.get("/logout")
async def logout(request: Request):
    """ログアウト。セッションを破棄してログイン画面へ戻る。"""
    request.session.clear()
    return RedirectResponse(url="/login", status_code=303)
```

### 14.5 コード：`app/api/routes/home.py`

```python
# =============================================================================
# ホームルーター
# =============================================================================
from fastapi import APIRouter, Depends, Request

from app.api.routes.deps import get_current_user
from app.core.templates import templates
from app.models.employee import Employee

router = APIRouter()


@router.get("/")
async def home(
    request: Request,
    current_user: Employee = Depends(get_current_user),  # ← ログイン必須のガード
):
    """ホーム画面。ログイン中社員の情報を表示する。"""
    return templates.TemplateResponse(
        request,
        "home.html",
        {"current_user": current_user},
    )
```

### 14.6 解説

- **`request.form()`**: HTML フォームの `application/x-www-form-urlencoded` データを取得する（`python-multipart` が必要）
- **`request.session`**: dict のように使える。`request.session["user_id"] = employee.id` で保存
- **`Depends(get_current_user)`**: この引数があるだけで、未ログインなら自動でログイン画面へリダイレクトされる
- **`status_code=303`**: リダイレクトに使用（POST 後の再送防止の意味もある）

---

## 15. ステップ12：アプリのエントリポイント（app/main.py）

### 15.1 役割

FastAPI アプリを組み立てる。ミドルウェア（セッション）の追加、ルーターの登録、エラーハンドラの設定を行う。

### 15.2 作成するファイル

`app/main.py`（`core/templates.py` はステップ11で作成済み）

### 15.3 コード

```python
# =============================================================================
# アプリケーションのエントリポイント
# -----------------------------------------------------------------------------
# uvicorn app.main:app で起動される。
# ・セッション管理のミドルウェア設定
# ・ルーターの登録
# ・エラーハンドラ（403 / 404）
# を行う。
# =============================================================================
from fastapi import FastAPI, Request
from starlette.middleware.sessions import SessionMiddleware

from app.api.routes import auth, home
from app.core.config import get_settings
from app.core.templates import templates

# 設定を読み込む
settings = get_settings()

# FastAPI アプリ本体を作成
app = FastAPI(title="勤怠管理システム", debug=settings.debug)

# ---------------------------------------------------------------------------
# セッション管理（署名付きクッキー方式）
#   secret_key : Cookie の署名に使う秘密鍵（.env の SECRET_KEY）
#   same_site  : lax（同一サイトのみ送信を基本とする。CSRF 対策の一助）
#   https_only : True にすると Secure 属性（HTTPS のみ送信）。開発時は False
#   注: HttpOnly 属性は自動で付与される（JavaScript から Cookie を読めない）
# ---------------------------------------------------------------------------
app.add_middleware(
    SessionMiddleware,
    secret_key=settings.secret_key,
    session_cookie="session",
    max_age=14 * 24 * 60 * 60,  # 14 日間有効（秒）
    same_site="lax",
    https_only=False,  # 本番（HTTPS）運用時は True にする
)

# ---------------------------------------------------------------------------
# ルーターの登録
# ---------------------------------------------------------------------------
app.include_router(auth.router)
app.include_router(home.router)


# ---------------------------------------------------------------------------
# エラーハンドラ
# ---------------------------------------------------------------------------
@app.exception_handler(403)
async def forbidden_handler(request: Request, exc):
    """権限エラー（403）を専用画面で表示する。"""
    return templates.TemplateResponse(request, "errors/403.html", status_code=403)


@app.exception_handler(404)
async def not_found_handler(request: Request, exc):
    """ページ不存在（404）を専用画面で表示する。"""
    return templates.TemplateResponse(request, "errors/404.html", status_code=404)
```

### 15.4 解説

- **`app.add_middleware(SessionMiddleware, ...)`**: すべてのリクエストでセッション機能を使えるようにする
- **`app.include_router(...)`**: auth.py・home.py で定義したルーターをアプリに組み込む
- **`@app.exception_handler(403)`**: `HTTPException(403)` が raise されたときに呼ばれる。403 テンプレートを返す
- **`@app.exception_handler(404)`**: 存在しない URL へのアクセスを 404 テンプレートで返す

---

## 16. ステップ13：シードデータ（app/seed.py）

### 16.1 役割

動作確認用の**ダミーデータ**を投入する。実在データは使わない（パブリックリポジトリのため）。

### 16.2 作成するファイル

`app/seed.py`

### 16.3 コード

```python
# =============================================================================
# ダミーデータ投入スクリプト
# -----------------------------------------------------------------------------
# 実行方法: .venv/bin/python -m app.seed
# 全件削除 → 再投入 の冪等な実装（何度実行しても同じ初期状態になる）。
# パブリックリポジトリのため、実在の人名・企業名は使わない。
# =============================================================================
from datetime import date, time

from sqlalchemy import delete

from app.core.security import hash_password
from app.db.session import SessionLocal, engine
from app.models import Base, Department, Employee, LeaveType, WorkSchedule


def seed_all() -> None:
    """ダミーデータを投入する。"""
    # テーブルが無ければ作る（マイグレーション未実行でも動作確認できるように）
    # ※通常は alembic upgrade head を使う。ここは学習用の保険。
    Base.metadata.create_all(engine)

    db = SessionLocal()
    try:
        # ---------- 既存データを全削除（子テーブル → 親テーブルの順） ----------
        db.execute(delete(Employee))
        db.execute(delete(LeaveType))
        db.execute(delete(WorkSchedule))
        db.execute(delete(Department))

        # ---------- 部署マスタ ----------
        dept_admin = Department(code="ADMIN", name="総務部", note="総務・人事を担当")
        dept_sales = Department(code="SALES", name="営業部")
        dept_eng = Department(code="ENG", name="開発部")
        db.add_all([dept_admin, dept_sales, dept_eng])

        # ---------- 勤務体系マスタ ----------
        std_schedule = WorkSchedule(
            code="STD",
            name="標準8時間勤務",
            start_time=time(9, 0),     # 09:00 始業
            end_time=time(18, 0),      # 18:00 終業
            required_minutes=480,      # 8 時間
            break_minutes=60,          # 休憩 1 時間
        )
        db.add(std_schedule)
        db.flush()  # id を確定させる（後続の社員から参照するため）

        # ---------- 休暇種別マスタ ----------
        leave_types = [
            LeaveType(code="PAID", name="年次有給休暇", grant_days=10, carryover=True, half_day=True),
            LeaveType(code="KEIGA", name="慶弔休暇", grant_days=5, carryover=False, half_day=False),
            LeaveType(code="SPECIAL", name="特別休暇", grant_days=3, carryover=False, half_day=True),
            LeaveType(code="KESSKI", name="欠勤", carryover=False, half_day=False),
        ]
        db.add_all(leave_types)

        # ---------- 社員マスタ（管理者1名 + 一般5名） ----------
        # パスワードは全員同じダミー値（bcrypt でハッシュ化して保存）
        password = hash_password("password123")

        # 共通項目（勤務体系・入社日・パスワード）はループ内でまとめて設定する
        employees_data = [
            # (社員番号, 姓, 名, 姓カナ, 名カナ, 部署, 雇用形態, ロール)
            ("E0001", "管理者", "太郎", "カンリシャ", "タロウ", dept_admin, "seishain", Employee.ROLE_ADMIN),
            ("E0002", "テスト", "太郎", "テスト", "タロウ", dept_eng, "seishain", Employee.ROLE_EMPLOYEE),
            ("E0003", "テスト", "花子", "テスト", "ハナコ", dept_eng, "seishain", Employee.ROLE_EMPLOYEE),
            ("E0004", "テスト", "次郎", "テスト", "ジロウ", dept_sales, "keiyaku", Employee.ROLE_EMPLOYEE),
            ("E0005", "テスト", "美咲", "テスト", "ミサキ", dept_sales, "arbeit", Employee.ROLE_EMPLOYEE),
            ("E0006", "テスト", "大輔", "テスト", "ダイスケ", dept_eng, "seishain", Employee.ROLE_EMPLOYEE),
        ]

        for (
            number, last, first, last_kana, first_kana,
            dept, emp_type, role,
        ) in employees_data:
            db.add(Employee(
                employee_number=number,
                last_name=last,
                first_name=first,
                last_name_kana=last_kana,
                first_name_kana=first_kana,
                department=dept,
                employment_type=emp_type,
                work_schedule=std_schedule,
                hire_date=date(2020, 4, 1),
                role=role,
                password_hash=password,
            ))

        # コミットして確定
        db.commit()
        print("シードデータを投入しました（社員 6 名）")
    finally:
        db.close()


if __name__ == "__main__":
    seed_all()
```

### 16.4 解説

- **冪等（べきとう）**: 何度実行しても同じ結果になる。全件削除 → 再投入
- **削除順序**: 外部キー制約があるため、子（Employee）→ 親（Department など）の順に削除する
- **`db.flush()`**: 主キー（id）を確定させる。その後 `work_schedule=std_schedule` のようにオブジェクトを渡すと、自動で外部キー値が入る
- **`department=dept`**: relationship を経由してオブジェクトを渡すと、`department_id` が自動設定される

### 16.5 実行

```bash
.venv/bin/python -m app.seed
```

実行時に `data/` ディレクトリが無いと失敗するので、先に作っておく（ステップ0.4）。

---

## 17. ステップ14：動作確認

### 17.1 開発サーバーの起動

```bash
.venv/bin/uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

- アプリ: http://127.0.0.1:8000
- ログイン画面: http://127.0.0.1:8000/login

### 17.2 確認手順

1. **ログイン画面が表示されるか**
   - http://127.0.0.1:8000/login を開く。ログイン画面が表示される
2. **正しい情報でログインできるか**
   - 社員番号 `E0001`・パスワード `password123`（管理者）でログイン
   - ホーム画面に「管理者 太郎 さん」「所属: 総務部」「ロール: 管理者」と表示される
3. **一般社員でもログインできるか**
   - 社員番号 `E0002`・パスワード `password123` でログイン
   - 「テスト 太郎 さん」「ロール: 一般社員」と表示される
4. **誤ったパスワードで失敗するか**
   - `E0001` + 誤ったパスワード → 「社員IDまたはパスワードが正しくありません」と表示される
5. **未ログインでホームにアクセスするとログイン画面へ飛ぶか**
   - ログアウト後、http://127.0.0.1:8000/ にアクセス → ログイン画面へリダイレクトされる
6. **存在しない URL で 404 画面が出るか**
   - http://127.0.0.1:8000/nothing を開く → 「404 - ページが見つかりません」が表示される

### 17.3 サーバーの停止

ターミナルで `Ctrl+C`。

---

## 18. 受け入れ条件チェックリスト

基本設計書 10 章の受入条件に沿って確認する。

| No | 受入条件 | 確認 |
|----|----------|:---:|
| AC-01 | `/login` にアクセスするとログイン画面が表示される | ☐ |
| AC-02 | 管理者アカウント（E0001 + password123）でログインできる | ☐ |
| AC-03 | 一般社員アカウント（E0002 + password123）でもログインできる | ☐ |
| AC-04 | 誤ったパスワードではログインできず、共通のエラーメッセージが表示される | ☐ |
| AC-05 | ログイン後にホーム画面（`/`）が表示され、氏名・部署・ロールが表示される | ☐ |
| AC-06 | 未ログインで `/` にアクセスすると `/login` へリダイレクトされる | ☐ |
| AC-07 | ログイン済みで `/login` にアクセスするとホームへリダイレクトされる | ☐ |
| AC-08 | ログアウトで `/login` へ戻り、未ログイン状態になる | ☐ |
| AC-09 | 存在しない URL で 404 画面が表示される | ☐ |
| AC-10 | 管理者でログインするとロールが「管理者」と表示される | ☐ |

---

## 19. よくあるつまずきと対処

| 症状 | 原因と対処 |
|------|------------|
| `ModuleNotFoundError: No module named 'itsdangerous'` | ステップ0 の requirements.txt への追記を忘れている。追記して `pip install -r requirements.txt` を再実行 |
| `ModuleNotFoundError: No module named 'app'` | カレントディレクトリがプロジェクトルートでない。`cd` でルートに移動してから実行する |
| マイグレーションで「テーブルが作られない」 | `alembic upgrade head` を実行していない。実行してから確認する |
| SQLite の「database is locked」エラー | ブラウザの別タブや別コマンドで DB を開いている。一度閉じて再実行する（開発中は 1 プロセスで確認する） |
| ログインしてもすぐログイン画面に戻る | `get_current_user` で社員を取得できていない。`app.seed` を実行しているか、`can_login()` の条件を確認する |
| フォーム送信後 400 の「入力してください」エラー | フォームの `name` 属性が `employee_number` / `password` と一致しているか確認する |
| `POST /login` で 422 エラー | セッションや CSRF ではなく、フォーム送信の形式の問題。ログイン画面の `<form>` に `action="/login"` があるか確認 |
| 静的ファイル（CSS）が効かない | Phase 0 ではテンプレート内に `<style>` を直接書く方式。静的ファイル配信は Phase 1 で導入 |

---

## 20. 次のステップ

Phase 0 が完了したら、次の順で進める。

1. **Phase 1（マスタ管理）**: 部署・社員・勤務体系・休暇種別の管理画面（CRUD）を実装する
   - このとき **CSRF 対策**（POST 送信の保護）を一括導入する
2. **Phase 2（打刻機能）**: `time_records` テーブルと打刻 API・画面、勤務時間の計算
3. **Phase 3（照会・修正）**: カレンダー表示・デイリー修正・修正履歴
4. **Phase 4（集計）**: 月次集計・CSV 出力

各フェーズを始める前に、要件定義書（docs/01）と基本設計書（docs/02）にセクションを追加し、詳細設計書を更新する。

---

*文書終わり*
