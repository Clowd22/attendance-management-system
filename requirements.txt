# =============================================================================
# requirements.txt
# -----------------------------------------------------------------------------
# このプロジェクトの「直接依存」するトップレベルパッケージの一覧。
# バージョンはインストール時に解決され、完全な依存ツリーは pip freeze で
# 確認できる。学習プロジェクトではパッケージの進化に追従しやすいように
# メジャーバージョン制約（>=x,<y）のみを指定している。
#
# インストール方法:
#   .venv/bin/pip install -r requirements.txt
# =============================================================================

# --- Web フレームワーク -------------------------------------------------------
# FastAPI: モダンな Python Web フレームワーク。型ヒントベースで API を定義する。
fastapi>=0.115

# Uvicorn: ASGI サーバー。FastAPI の実行環境。standard 版は WebSocket 等に必要。
uvicorn[standard]>=0.30

# Jinja2: サーバーサイドテンプレートエンジン。画面(HTML)をサーバー側で生成する。
jinja2>=3.1

# python-multipart: FastAPI で HTML フォーム(application/x-www-form-urlencoded)を
# 受け取るために必要。
python-multipart>=0.0.9

# --- ORM / データベース -------------------------------------------------------
# SQLAlchemy: Python の代表的な ORM。DB を Python オブジェクトとして操作する。
sqlalchemy>=2.0

# Alembic: SQLAlchemy 用のマイグレーションツール。DB スキーマのバージョン管理を行う。
alembic>=1.13

# --- 認証・セキュリティ -------------------------------------------------------
# pwdlib: パスワードのハッシュ化ライブラリ（passlib の後継・FastAPI公式推奨）。
#          Argon2 など複数のアルゴリズムをサポート。bcrypt バックエンドを使用する。
#          注意: 旧来の passlib はメンテナンス停止しており、新しい bcrypt と
#                互換性が崩れているため採用しない。
pwdlib[bcrypt]>=0.2

# --- 設定・ユーティリティ ------------------------------------------------------
# pydantic-settings: .env ファイルや環境変数から設定を読み込む。
pydantic-settings>=2.3

# python-dateutil: 日付・時刻の高度な計算（勤務時間の集計などで使用）。
python-dateutil>=2.9

# --- 開発・テスト（後で追加予定） ----------------------------------------------
# pytest / httpx はフェーズが進んだら追加する。
