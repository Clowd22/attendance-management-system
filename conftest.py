# =============================================================================
# pytest の共通設定（conftest.py）
# -----------------------------------------------------------------------------
# pytest はこのファイルを見つけると、このディレクトリ（プロジェクトルート）を
# sys.path に追加する。これによりテスト内の `from app.xxx import ...` が通る。
# テスト全体で共通の fixture も、将来ここに置く。
# =============================================================================
