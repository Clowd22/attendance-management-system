# =============================================================================
# .gitignore
# -----------------------------------------------------------------------------
# このプロジェクトで git の管理対象から除外するファイルの一覧。
# 特にこのリポジトリは「パブリック」なので、
#   1. 秘密情報（APIキー、パスワード等）が入ったファイル
#   2. 個人情報（実在の勤怠データ等）
#   3. 再生成可能なファイル（仮想環境、キャッシュ、DBファイル等）
# は絶対にコミットしてはいけない。
# =============================================================================

# ---------------------------------------------------------------------------
# 環境変数・秘密情報
#   - シークレット情報が入るファイル。テンプレートだけは .env.example で管理する。
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key
*.p12

# ---------------------------------------------------------------------------
# Python 仮想環境
#   - どのマシンでも requirements.txt から再現できるため管理対象外
# ---------------------------------------------------------------------------
.venv/
venv/
env/
ENV/

# ---------------------------------------------------------------------------
# Python のバイトコードキャッシュ
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so

# ---------------------------------------------------------------------------
# テスト・カバレッジ関連のキャッシュ
# ---------------------------------------------------------------------------
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# ---------------------------------------------------------------------------
# SQLite データベースファイル
#   - 勤怠・人事データが入るためコミットしない。
#   - テストデータは「スクリプト（seed）で再生成する」方針とする。
# ---------------------------------------------------------------------------
*.db
*.db-journal
*.sqlite
*.sqlite3
db/*.sqlite3
data/*.db

# ---------------------------------------------------------------------------
# エディタ / OS の一時ファイル
# ---------------------------------------------------------------------------
.DS_Store
*.swp
*.swo
.idea/
.vscode/
*.log

# ---------------------------------------------------------------------------
# アプリ実行時の一時アップロード等
# ---------------------------------------------------------------------------
uploads/
tmp/
